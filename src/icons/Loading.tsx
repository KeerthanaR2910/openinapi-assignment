import {ClipLoader} from "react-spinners";

const Loading = () => {
    return (
    <ClipLoader
        color={"white"}
        loading={true}
        size={20}
    />
    )
}

export default Loading